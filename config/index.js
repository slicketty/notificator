/**
 * Config files
 */

const assets = require('./assets')
const errors = require('./errors')
const log = require('./log')
const routes = require('./routes')

module.exports = { assets, errors, log, routes }
